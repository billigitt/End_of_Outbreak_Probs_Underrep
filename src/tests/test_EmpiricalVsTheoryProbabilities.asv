%test empiricalInf0s with theory

I = randi(10, 1, 5)';

w = [0.1 0.2 0.3 0.4]';

R = 1;
numExp = 300;
Tinf = 500;

prob0sEmp = empiricalInf0s(I, w, R, numExp, Tinf);

gamma = gamma_tMostBasic([I; zeros(Tinf)], w, length(I));
prob0sTheory = exp(-R*gamma);
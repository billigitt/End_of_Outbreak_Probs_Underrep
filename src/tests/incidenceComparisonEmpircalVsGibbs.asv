close all
clc
clear all

load('../mats/GibbsApproach1-SuperSimple-N1e6-CompareWithWrongEmpirical')



tt=16;

for t = 1:41

    if t+9 >= tt

        figure
        h(1) = histogram(simulationOutput(t+9).cellOfMatrices(15, :), 'BinWidth', 1, 'Normalization','probability');
        hold on
        h(2) = histogram(outputGibbs(t).incidenceStore(15, :), 'BinWidth', 1, 'Normalization','probability');

        legend(h([1 2]), 'sims', 'gibbs')

    end

end
